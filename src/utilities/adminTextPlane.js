/*const child_process = require('child_process');

export default function execCommandCMD() {
    var command = child_process.spawn('cmd', ['/c', 'dir C:\\Windows\n']);
    command.stdout.on('data', function (dat) {
        console.log('salida: ' + dat.toString())
    });

}*/